export enum PriceType {
  VISIT = 'VISIT',
  HOURLY = 'HOURLY',
  DAILY = 'DAILY',
  PER_UNIT = 'PER_UNIT',
  FIXED = 'FIXED',
}

export enum PriceComputationMode {
  FLAT = 'FLAT',
  PER_BLOCK = 'PER_BLOCK',
  PER_MINUTE = 'PER_MINUTE',
}